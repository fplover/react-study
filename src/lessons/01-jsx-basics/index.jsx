/**
 * 第 01 关 · JSX 与元素
 * ------------------------------------------------------------
 * 目标:认识 JSX,学会用 {} 在标签里嵌入 JS 表达式。
 * 打开本文件,按照 👉 TODO 提示动手修改,右边保存后页面会自动刷新。
 */

export default function JsxBasics() {
  const name = '同学' // 👉 TODO 1:把这里改成你自己的名字
  const isMorning = new Date().getHours() < 12

  return (
    <div className="demo-card">
      {/* JSX 里的注释要用大括号包起来,像这样 */}
      <h2>你好,{name}!👋</h2>

      <div className="mini-card">
        <h3>① 用 {'{}'} 显示表达式</h3>
        <p>
          {/* 👉 TODO 2:把下面这行改成显示 1 + 2 的结果(用大括号包一个表达式) */}
          1 + 2 = <b>{'?'}</b>
        </p>
        <p>
          今天是 <b>{new Date().toLocaleDateString('zh-CN')}</b>,大括号里可以放任意表达式。
        </p>
      </div>

      <div className="mini-card">
        <h3>② 根据条件显示不同文字</h3>
        <p>
          {/* 👉 TODO 3:用三元表达式,isMorning 为 true 显示"早上好",否则"你好" */}
          现在应该说:<b>{'（在这里用三元表达式）'}</b>
        </p>
      </div>

      {/* ------------------------------------------------------------
          参考答案(先自己试,再对照):
          TODO2:  1 + 2 = <b>{1 + 2}</b>
          TODO3:  <b>{isMorning ? '早上好' : '你好'}</b>
         ------------------------------------------------------------ */}
    </div>
  )
}
